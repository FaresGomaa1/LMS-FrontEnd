import { ResultStyleDirective } from './result-style.directive';

describe('ResultStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new ResultStyleDirective();
    expect(directive).toBeTruthy();
  });
});
